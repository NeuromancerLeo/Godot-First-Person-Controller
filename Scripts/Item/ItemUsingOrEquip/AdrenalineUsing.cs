using Godot;
using System;

/// <summary>
/// 肾上腺素的使用方法
/// </summary>
public partial class AdrenalineUsing : Node
{
    /*private PlayerBasicMovement _player;
    double timer = 0;
    double duration = 10;

    public override void _Ready()
    {
        GD.Print("Adrenaline节点被成功实例化了！");
        _player = GetNode< PlayerBasicMovement > ("..");
        _player.walkingSpeed += 1;
        _player.sprintingSpeed += 2;
    }



    public override void _PhysicsProcess(double delta)
    {
        GD.Print(timer);
        if(timer < duration)
        { 
            //_player.Velocity = _player.Velocity * 1.1f;
            timer += delta;
        }else
        {
            _player.walkingSpeed -= 1;
            _player.sprintingSpeed -= 2;
            this.QueueFree();
        }    
      
    }*/
}

